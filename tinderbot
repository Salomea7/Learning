#If you dont already have pyautogui installed, open command prompt as administrator and type
#"pip install PyAutoGUI" and install it.  Then open the python file.

import pyautogui
import time

n = input("How many Likes do you want to give? ")
input("Place cursor over Like button on the Tinder page and press enter ")
m = pyautogui.position()

int_n = int(n)
i = 0
while i < int_n:
	pyautogui.click(m)
	time.sleep(0.4)
	i +=1
